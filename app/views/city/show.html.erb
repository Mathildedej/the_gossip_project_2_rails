<html>
  <head>
    <title>The Gossip Project</title>
  </head>
  <body>
    <br />
    <div class = "text-center"><h4> Voici la liste actuelle des potins écrits par les auteurs de la ville <%= @city.name%>: </h4></div>
    <br />
    <div class="container-potin row justify-content-center">
    <% id_potin = 1%>
    <% @gossips.each do |gossip| %>
      <div class="card mt-2 text-center" style="width: 20rem;">
        <h6 class="card-header"><%= User.find_by(id:gossip.user_id).first_name %></h6>
        <div class="card-body">
          <h6 class="card-title"><%= gossip.content %></h6>
          <a href="<%= gossip_path(gossip.id) %>" class="btn btn-primary">Gossip n° <%=id_potin%></a>
        </div>
      </div>
      <% id_potin += 1%>
    <% end %>
  </div>


  </body>
</html>